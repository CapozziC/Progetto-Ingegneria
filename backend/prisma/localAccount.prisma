model LocalAccount {
  id  Int      @id @default(autoincrement())
  passwordHash      String
  authDate          DateTime?
  authTime          DateTime?

  userId          Int      @unique
  user            User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("local_account")
}

