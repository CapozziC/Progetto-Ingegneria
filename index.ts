/*import { createUserController } from "./backend/controllers/user.controller";

async function main() {
  const user = await createUserController({
    email: "carlacapozzi@gail.com",
    name: "Carla",
    surname: "Capozzi",
  });
  console.log("New user created:", user);
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});*/
